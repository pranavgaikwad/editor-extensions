# MTA brand configuration for E2E tests
# This file can be copied to .env in downstream repos

# Brand configuration for MTA
EXTENSION_NAME=mta-vscode-extension
EXTENSION_PUBLISHER=redhat
TEST_CATEGORY=MTA

# Other test settings (inherit from .env or set as needed)
# OPENAI_API_KEY=''
# VERBOSE=false
# TEST_DATA_DIR='test-data-dir'
# etc...